# токен от @BotFather — обязательный
BOT_TOKEN=

# твой Telegram ID — куда приходят заявки и отзывы на модерацию
# (уже стоит значением по умолчанию в config.py, переменную можно не задавать)
ADMIN_ID=537259850

# каталог для базы leads.db, переживающий передеплой (на bothost.ru: /app/data)
# без переменной база лежит рядом с кодом и обнуляется при каждом пуше
DATA_DIR=
